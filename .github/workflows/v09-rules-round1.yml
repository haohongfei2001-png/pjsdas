name: V09 Rules Round 1

on:
  push:
    branches: [main]
    paths:
      - 'scripts/v09-rules-round1.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Decision Rules patch
        run: python3 scripts/v09-rules-round1.py
      - name: Verify
        run: |
          npm ci
          npm test
          npm run build
      - name: Commit product changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src tests
          git commit -m "Add editable Decision Rules engine"
          git push
