name: Verify authenticated gateway readiness

on:
  push:
    paths:
      - '.github/workflows/verify-auth-readiness.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check production auth readiness
        shell: bash
        run: |
          set -euo pipefail
          body=$(curl -sS https://pjsdas-remote-alpha.vercel.app/api/health-auth)
          echo "$body"
          echo "$body" | jq -e '.ready == true and .secretsConfigured.tokenEncryptionKey == true and .secretsConfigured.googleClientId == true and .secretsConfigured.googleClientSecret == true'
