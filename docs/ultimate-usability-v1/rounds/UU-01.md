# UU-01 — Time and Recruiting Node Model

State: **READY — NOT STARTED**

Baseline Browser E2E closure is complete on main; this round is authorized to begin in a separate one-round execution.

Implement additive ScheduleNode/occurrence identity, orthogonal process progress/result/participation,
temporal precision/timezone semantics, and migration compatibility projections.

Exit requires round-trip history preservation plus golden tests for date-only, DST, timezone,
multiple occurrences, elapsed-unresolved and supersession.

Native-client compatibility is a hard gate.
