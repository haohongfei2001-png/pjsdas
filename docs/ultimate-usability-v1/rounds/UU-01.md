# UU-01 — Time and Recruiting Node Model

State: **COMPLETE**

Implementation: `main@ebff7072dab9063eb1dbf8986c257e3307f9b631` via PR #96.

## Delivered

- Snapshot v2 + additive v1→v2 reader/migration compatibility.
- canonical ScheduleNode with stable occurrence identity and versioned supersession.
- temporal precision/timezone/date-only/fixed-range/deadline semantics.
- orthogonal process stage / progress / result / participation state.
- elapsed-without-evidence → `elapsed_unresolved`, never implicit completion.
- legacy deadline/dueAt compatibility projections.
- IndexedDB v9 `scheduleNodes` persistence and bounded backfill.
- connected workspace readers/writers upgraded to v2 without bypassing CAS/idempotency/ledger.
- production schema-downgrade guard deployed to Supabase before v2 rollout.

## Exit evidence

- date-only, DST/offset, multiple-occurrence, elapsed-unresolved, supersession and history
  round-trip golden tests pass.
- CI/build: `35504952185` — success.
- Chromium: `35504952158` — success.
- exact-SHA backend + Pages/deploy: `35504952218` — success.
- Production Self-Test: `35505035526` — success.
- release workflow: `35505049649` — success / publication disarmed.
- Supabase migration: `20260920102225 schedule_node_snapshot_v2`; downgrade trigger verified.

The existing production workspace was not bulk-rewritten. It remains readable as v1 and upgrades
through the v2 compatibility layer on normal use.

Native-client compatibility remains a gate: ScheduleNode and process semantics are platform-neutral.

## Stop boundary

UU-01 is closed. Do not implement UU-02 in this execution.
