# UU-02 — Unified Semantic Intake and Write Policy

State: **COMPLETE**

Implementation: `main@e9236676ab678d9847d581ef6f2dfdc9ba34b1e5` via PR #97.

## Delivered

- source-neutral Semantic Intake v1 for Web, current-chat MCP, owner PAIA/ChatGPT, structured Gmail,
  and future iPhone adapters;
- deterministic Opportunity / ScheduleNode occurrence resolution with fail-closed ambiguity;
- durable DecisionRequest + SemanticReceipt contracts;
- atomic occurrence completion and rescheduling through shared domain commands;
- Owner Amendment OA-03 internal abandonment policy with shared-governance DecisionRequest guard;
- statement-mode guard preventing questions/quotes/examples/hypotheticals/rewrite text from becoming
  business facts;
- server-side source authorization and transactional CAS/idempotent command-ledger writes;
- latest-revision field/object compensation Undo, never stale whole-snapshot restore;
- Snapshot v3 and IndexedDB v10 persistence;
- MCP tool surface v4 with semantic_intake / resolve_semantic_decision / undo_semantic_command;
- production authorization vocabulary extended for semantic_intake without automatically granting it.

## Exit evidence

- 625 tests: pass.
- CI/build: `35512877012` — success.
- Chromium: `35512877114` — success.
- exact-SHA backend + Pages/deploy: `35512877034` — success.
- Production Self-Test: `35512961598` — success.
- release workflow: `35512976882` — success / publication disarmed.
- Supabase migration: `20260920131307 semantic_intake_policy`; constraint verified and semantic
  grants remained zero.

The existing production workspace was not bulk-rewritten and no new external-source permission was
silently enabled.

Native-client compatibility remains intact: Semantic Intake, DecisionRequest, receipts and stable
object/occurrence identities are platform-neutral.

## Stop boundary

UU-02 is closed. Do not implement UU-03 in this execution.
