# UU-06 — Gmail and Discovery Intake Closure

State: **IN_PROGRESS**
Execution: `UU06-20260921-aem01`
Current canonical publication writer: AEM manager
Runtime implementation writer: AEM executor `/root/pjsdas_uu06` (publication checkpoint delivered)
Implementation branches: `manager/uu06-aem-20260921` (merged PR #102),
`manager/uu06-idle-scheduler-20260921` (merged PR #103)
Deployed runtime: `main@107795acbb17dbbd17ea9e5f1963c39d39a91957`
Execution baseline: `main@bb1cabf30ae843ee97a0865bbce05d61d3ade25c`

Canonical STATUS already made this round READY after UU05. The old NOT_READY line
was a static template. The new owner authorization starts this independent execution;
it does not reuse the UU05 writer lease or authorize UU07.

## Acceptance map

- Gmail: read-only existing opt-in only; broader bounded initial backfill and archived
  messages, complete-consumption pagination/watermark; history with scheduled compensation.
- Structured multi-event observations must use shared semantic policy/domain commands,
  retain occurrence identity, precision, receipts, replay safety and stale-event protection.
- Attachments/link support and missing-capability gaps must be explicit and visible.
  No permission, credential, paid infrastructure or external recruiting action expansion.
- Discovery: verify existing independent public-source verification, identity dedupe,
  quality gate and stale/changed-source reconciliation before adding any code.
- Gate: meaningful unit regressions, browser safety, build, exact candidate checks;
  manager review, remote main, exact-main deployment/self-test, canonical evidence closure.
- Release publication remains DISARMED. Private email data is not test evidence.

## Initial audit

At the execution baseline, Gmail scans were seven-day INBOX-only, body parsing was
single-event, and attachments were silently ignored. History continuation already avoids skipping unconsumed
pages/ids; preserve that mechanism. Discovery already independently verifies source facts.
Pub/Sub push is not configured in the existing deployment and must not be represented as live.

## Current deployment checkpoint — not round closure

Status remains **IN_PROGRESS**. UU07 is not authorized by this execution.
PR #102 and #103 are merged; current runtime 107795a includes the intake/control
implementation and idle-scheduler correction. Both prerequisite schemas and the
idle-scheduler migration are applied. The execution-control flag stays default-off;
Gmail hourly cadence and Discovery stay unchanged, while Gmail now checks eligibility
before HTTP enqueue. Live Gmail SLOs remain unverified. See the final checkpoint and
[deployment evidence](../evidence/UU-06-runtime-072b2fe.md) below.
Earlier candidate results in this document remain historical evidence, not current
claims that the deployed schemas or runtime are absent.

### Consent and deployment boundary

The original candidate was rejected by automatic approval review because it would
have expanded every existing enabled Gmail binding from seven-day INBOX reads to
90-day archive-inclusive reads and new structured location/link storage. That
candidate must not be published. This revision replaces that implicit expansion:

- `FROZEN_BLUEPRINT.md` section 13 requires broader first-use recruitment backfill,
  outside-INBOX coverage, and structured time/location/link/deadline extraction.
  It does not authorize upgrading consent of existing users. 90 days is a bounded
  implementation parameter, not a literal owner instruction.
- `GMAIL_AUTOMATION.md` requires the user explicitly enable Gmail read-only access.
  The new first-party opt-in now explains 90 days, archived mail, excluded spam/trash,
  and necessary structured recruiting locations/meeting links. Only that actual
  interaction supplies consent version `uu06-v1`; generic enable requests, existing
  enabled rows, OAuth scope, missing cursor and old pending OAuth flows do not.
- The applied additive consent migration defaults every row to NULL and enables no
  user. It creates no scheduler, secret, scope or paid resource.
- Missing/old consent keeps the baseline legacy parser and seven-day INBOX initial,
  fallback and history behavior. Legacy continuation tokens remain unchanged.
- Disabling, account/credential replacement, scope removal, revocation and terminal
  OAuth failures clear the new consent. Only a transition from an expanded grant
  also clears its unconsumed queue/page state; committed history/evidence survive.
- Missing v3 claim RPC may fall back to v2 with expanded consent forced absent.
  Authorization failures never trigger fallback. Old settings work before migration;
  new expanded requests return an explicit not-deployed response instead of pretending
  to save consent. Schema must therefore precede deployment of the new opt-in UI.

### Contract map and remaining gates

| Contract | Candidate evidence / actual limit |
| --- | --- |
| Archive-inclusive bounded first-use pagination | Explicit version only; stable 90-day query across pages, full-consumption watermark retained; old users preserve original range. |
| Multi-event / subject context | Shared semantic pipeline handles multiple distinct events and subject-only invitation context; quoted/example text never becomes an automatic fact. |
| Structured time/deadline | Valid full dates and explicit Shanghai date-times preserved; impossible dates rejected. Explicit today/tomorrow/day-after and this/next weekday expressions resolve from original source timestamp/timezone; impossible, missing-source-time, ambiguous zone/DST and unsupported yearless expressions remain visible decisions. |
| Structured location/link | Bounded `ProcessEvent.location` / `joinUrl`; HTTPS links only, no embedded credentials, necessary user-facing notes label link unverified. This is extraction from authorized message text, not URL fetching. |
| Attachment / linked-page fetching | **NOT_SUPPORTED**, visible coverage gaps; no invented contents, OAuth scopes or external reads. This does not claim a decisive unsupported attachment is resolved. |
| Completion / cancellation / reschedule | Shared domain commands, stable occurrence identity, reversible cancellation, multi-source replay safety, late assertions become explicit conflicts instead of regressing newer facts. |
| Coverage and privacy | Durable per-message gaps and receipts, source consumption accounting, no raw body persistence, no external application/submission/withdrawal. |
| Discovery | Existing public-source independent verification and identity/quality guards retained; targeted regression suite passes. No duplicate implementation. |
| Gmail normal p95 <2 min / lost-push <=15 min | **UNMET/UNMEASURED**. Baseline activation migration `2026091904_activate_automation_scheduler.sql` schedules `pjsdas-gmail-automation-hourly` at `5 * * * *` (older GMAIL_AUTOMATION.md says minute 7); source registry cadence is 60 min. Applied idle-scheduler migration adds eligibility only, not a frequency change. No push deployment or real latency evidence. Do not label this a pass or invent infrastructure. |
| Full acceptance matrix | G14 original-time relative replay and G21 distinct availability-window/deadline now have synthetic domain and real browser/IndexedDB evidence. Unsupported ambiguous forms remain clarification; live-source performance certification is still outstanding. No full-round completion claim. |
| Publication / exact SHA CI / deployment | PR #102/#103 merged; current runtime main 107795a, both schemas and idle-scheduler migration applied; exact-main receipts are in the latest checkpoint. Execution-control flag stays off; final release publication stays disarmed. This increment is published, not full-round COMPLETE. See latest evidence below. |

### Validation

Synthetic tests cover Gmail paging/replay, shared identity/time/conflict commands,
new/old consent and OAuth pending versions, pre-migration compatibility, and actual
PostgreSQL migration transitions (including revoked expanded pending state and
preserved legacy continuation). The headless browser journey exercises actual
IndexedDB persistence, Today rendering and reload; no private Gmail data is used.
Full-suite totals and immutable candidate SHA are recorded in the PR after publication.

Historical initial candidate verification: **703/703 unit tests PASS**, TypeScript and production build PASS.
Independent authorization-chain review accepted the materially restricted candidate;
remote automatic approval and CI were separate required checks, subsequently passed
on the corrected candidate and runtime SHA recorded below.


## Temporal follow-up checkpoint

- The shared source temporal resolver uses original message timestamp and configured
  source timezone, not ingestion time. Explicit week/day expressions survive delayed
  replay; missing timestamps, impossible dates and DST ambiguous/nonexistent wall
  times fail closed. RawExpression contains only the bounded date/time expression.
- The shared `process_event` contract accepts existing ScheduleNode temporal shapes;
  availability windows and submission deadlines from separate message fragments
  become distinct nodes, with stable identities, replay, conflict and Undo behavior.
  Changed window endpoints do not silently replay as unchanged. No reschedule command
  is used to create these nodes. Today displays both window bounds and deadline labels.
- Initial CI passed at `45b2b02122aaeba6ba92cd3cee64ecae3914911e`; Browser E2E was
  33/34 because the new consent copy omitted existing explicit safety statements.
  Those statements were restored; assertions were preserved. Failure remains in
  remote history. Temporal follow-up local suite: 709/709 PASS, build PASS, three
  targeted headless journeys PASS including visible window bounds and deadline labels.
- At this temporal checkpoint production verification was still pending. Runtime
  deployment is now verified below; Gmail latency SLO remains unverified, round IN_PROGRESS.

## Independent temporal review follow-up

`829fd3cd159fec545d6d142bc41d063337b400e5` passed remote CI and Browser E2E.
Independent review then caught cases beyond the initial fixtures: Sunday synonym
`周天`, unsupported explicit Z/IANA/zone abbreviations, Date-header fallback using
scan time for assertion metadata, and rescheduling a window as a fixed appointment.
The follow-up preserves original source time for every candidate timestamp, retains
full temporal shapes on reschedule, treats 日/天 as Sunday, and asks for clarification
rather than interpreting an unknown explicit timezone as Shanghai. Missing original
message timestamps also require clarification, including application confirmations.
No prior candidate or failed workflow history was removed. Targeted regressions and
the 709-test suite passed locally. Corrected candidate 62ad547 passed its own
remote checks before the later 717-test controls increment and final runtime deployment.

## Dormant execution-control increment

The same UU06 execution adds a default-off `PJSDAS_GMAIL_EXECUTION_CONTROLS` path;
this does not change production cron, enable any mailbox or certify live SLOs.
The now-deployed migration and worker implement per-binding expiring leases,
coalescing, fresh claimed cursor state, budgeted source I/O, a write-before-commit
ownership check, and atomic fenced cursor/aggregate-metrics finish. Historical
backfill/recovery and replayed messages are excluded from new-history latency samples.

Independent review found that an active-only fence allowed an old v2 writer to
replace a cursor after a controlled finish. The candidate now persistently fences
both old v1/v2 state writers for bindings that have entered controlled mode; untouched
legacy bindings remain compatible. Safe activation/rollback prerequisites and the
limits of last-run metrics are in `docs/GMAIL_EXECUTION_CONTROLS.md`.

Local validation: 717/717 unit tests and build passed. Actual migration SQL passed
synthetic PostgreSQL tests for token denial, RLS/table denial, coalescing, expiry/
takeover, old-owner finish rejection, delayed legacy writes after release/expiry,
error cursor preservation and telemetry field allowlisting. The reproducible harness
is `scripts/verify-gmail-execution-controls.mjs` (isolated pinned PGlite 0.3.14).
The corrected candidate 9757739 subsequently passed independent review and its own
remote CI/browser checks; merged runtime 072b2fe also passed the exact-main gates below.

The manager has verified the linked Supabase organization plan as **free**, extracting
only that field and retaining no billing details. Actual usage/quota headroom and other
backend cost headroom remain **UNKNOWN**. The verified plan label is not evidence of
available capacity and does not justify raising cadence by itself.

## Historical schema-first and legacy ACL checkpoint

In the first schema step, the manager applied only the unchanged consent SQL from remote `8cb16700f76086f06b80b9193507aee5b13adab2`
as production migration `20260921053337` / `gmail_uu06_explicit_consent`.
Manager live readback: default NULL, enabled bindings **0**, expanded grants **0**,
RLS enabled, one consent trigger, and `claim v3(NULL)` rejected with SQLSTATE `42501`.
This enables no mailbox and changes no cron, environment flag, main or release state.
At that intermediate step controls remained unapplied; the corrected controls
migration was applied subsequently as recorded in the final checkpoint below.

The same live metadata audit found legacy state v1's production EXECUTE ACL was
owner/service_role only, stricter than the historical repository migration. The
controls candidate now explicitly revokes PUBLIC/anon/authenticated access to v1
while preserving its existing service_role privilege and internal persistent fence.
V2's existing token-gated anon compatibility is unchanged. The isolated PostgreSQL
harness models this production-style baseline and asserts ACLs before/after migration,
actual browser-role rejection, service_role legacy access and late-writer rejection.
Independent review and the SQL harness passed before production controls SQL was applied.

Pre-fix candidate `8cb16700f76086f06b80b9193507aee5b13adab2` passed exact remote
CI `35563941374` (717/717 and build) and Browser `35563941356` (35/35).
The ACL correction `9757739fddeed5fc21a044fa8841d5c09b1f4869` then passed its own
CI `35565347456` (717/717 and build) and Browser `35565347478` (35/35). Earlier
results are not used as evidence for the corrected tree.


## Deployed increment checkpoint — 2026-09-21

Runtime main `072b2feffdac8b72bc93b1e9701bdc8577b6eea0` is deployed after PR #102:

- CI `35566395645`: 717/717 and build PASS.
- Browser `35566395623`: 35/35 PASS.
- Deploy `35566395668`: SUCCESS.
- Production Self-Test `35566493535`: SUCCESS on canonical todayaction.com; the optional
  authenticated tool-list check is explicitly skipped, not claimed as live private-data verification.
- Release workflow `35566509739`: SUCCESS with release creation SKIPPED; publication disarmed.

Both consent `20260921053337` and corrected execution controls `20260921055432` are
applied. Manager live readback verified default-NULL consent; zero enabled bindings,
expanded grants and execution rows; RLS and anonymous table denial; preserved restricted
v1 EXECUTE ACL and unchanged v2 ACL; NULL worker tokens rejected with SQLSTATE 42501.
Source migration filenames and production history versions differ and are recorded
separately. Runtime health and frontend manifests match 072b2fe with the expected MCP
contract and migration-set digests. Full URLs/digests are in the
[deployment evidence](../evidence/UU-06-runtime-072b2fe.md).

This completes the implementation publication checkpoint only. The execution-control
flag remains off, hourly cron remains unchanged, no live opt-in was performed, and
normal p95/compensation certification remains UNMET/UNVERIFIED. Keep UU06 and its
execution IN_PROGRESS; do not start UU07 or present this as full acceptance/round closure.

## Idle-scheduler deployed checkpoint — 2026-09-21

PR #103 merged at current runtime `107795acbb17dbbd17ea9e5f1963c39d39a91957`.
The corrected scheduler migration is applied as `20260921065934`; consent
`20260921053337` and controls `20260921055432` remain applied. The first apply attempt
failed at an unnecessary `FOR UPDATE` with SQLSTATE 42501 and changed no job/history;
the correction uses existing `cron.alter_job` permissions with no new grants.

Gmail now enqueues HTTP only if an enabled non-revoked binding exists, including
legacy NULL-consent users. Job 15 remains active at `5 * * * *`; Discovery job 16 and
its `15 * * * *` cadence/command digest are unchanged. A real repeatable-read zero-eligible
transaction returned zero rows with no queue increase; natural 07:05 pg_cron execution
succeeded and its bounded response classification was `0 rows`, not literal `SELECT 0`.
No command/Vault value or private mail was output, and no opt-in was created.

Exact-main CI `35570999524` (717/717 and build), Browser `35570999455` (35/35),
deploy `35570999520` and Production Self-Test `35571134736` passed. Release
`35571158549` passed with creation skipped. Canonical health/frontend identities match
107795a. Full SQL limitations, failed attempt, run links and digests are in
[the idle-scheduler report](../../GMAIL_IDLE_SCHEDULER.md).

This completes only the bounded idle-scheduler checkpoint. It does not complete UU06
or its execution: cadence remains hourly, controls flag remains off, live SLO is
UNMET/UNVERIFIED and usage/cost headroom unknown. Do not enable Gmail for the user or
start UU07 to manufacture completion evidence.


## Gmail Push owner authorization checkpoint — 2026-09-21

The owner explicitly authorized UU06 to add the minimum official Gmail API Push
infrastructure inside the existing Google Cloud project:

- Cloud Pub/Sub topic/subscription and minimum IAM only;
- Gmail scope remains `gmail.readonly`;
- no broader user-data authority or external recruiting action;
- existing history/cursor/lease/Semantic Intake remains authoritative;
- compensation sync must be no slower than 15 minutes;
- any actual paid upgrade or new paid commitment requires a new owner decision;
- this execution remains UU06 only and must not start UU07.

Candidate PR #104 implements the bounded architecture on
`manager/uu06-gmail-push-20260921`. It is **NOT DEPLOYED** at this checkpoint.
No Google Cloud resource, mailbox opt-in, production migration, scheduler cadence
change or execution-control activation has been performed by this candidate.

The candidate uses authenticated Pub/Sub only as a wake signal. The push payload
cannot mutate recruiting truth; it can only enqueue the existing Gmail worker for
one already-enabled explicit-consent binding. That worker continues to read Gmail
history and pass observations through the existing Semantic Intake and mutation
guards.

The historical hourly Gmail job is proposed to become a ten-minute compensation
path, leaving five minutes of margin against the frozen <=15-minute missed-push
recovery ceiling. A separate daily job renews `users.watch`. Discovery is
unchanged. See `docs/GMAIL_PUSH.md`.

Round state remains **IN_PROGRESS**. Live p95 <2-minute evidence, lost-push
compensation evidence, real Google Cloud configuration, authenticated watch/push
canary and exact-main deployment evidence are still required before UU06 can close.
