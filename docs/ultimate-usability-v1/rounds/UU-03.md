# UU-03 — TodayBrief and Agenda Read Model

State: **COMPLETE**

Implementation: `main@dc098d15dca20f9552d50ffee5f285d68077c6e0` via PR #98.

## Delivered

- canonical revision-bound TodayBrief v1 for Web / MCP / future iPhone;
- one next action + at most three next-up actions;
- ScheduleNode-backed seven-calendar-day agenda;
- relevant DecisionRequests and material source-coverage warnings;
- fixed-event protection and quiet Today behavior;
- date-only precision preservation with no invented clock time;
- elapsed-unresolved recovery projection;
- latest-start hard-deadline protection and capacity-conflict visibility;
- explicit executability / real-target metadata;
- deterministic recommendations for identical revision/clock/timezone/rules inputs;
- authenticated MCP v5 `get_today_brief`, release-required for both connected authorities;
- legacy `get_today_plan` retained only for compatibility.

## Exit evidence

- 637 tests: pass.
- CI/build: `35514965754` — success.
- Chromium: `35514965802` — success.
- exact-SHA backend + Pages/deploy: `35514965767` — success.
- Production Self-Test: `35515043484` — success.
- release workflow: `35515057084` — success / publication disarmed.

No schema migration or production-data rewrite was required in UU-03.

Native-client compatibility remains intact because TodayBrief contains platform-neutral object
identities, timing semantics, decision payloads and deep-link targets rather than Web-only rules.

## Stop boundary

UU-03 is closed. Do not implement UU-04 in this execution.
