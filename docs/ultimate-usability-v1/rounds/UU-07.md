# UU-07 — ChatGPT / PAIA / Reminder Integration

State: **READY**

Operationalize owner PAIA first-class intake, current-chat MCP contracts, ReminderIntent/outbox,
external task/calendar capability probing, cross-source dedupe and single delivery ownership.
Unsupported capabilities must degrade truthfully.


Readiness note — 2026-09-22: UU-06 is complete. This round is READY but NOT_STARTED. No execution authority is implied by readiness.


## Execution checkpoint — 2026-09-22

Owner explicitly authorized UU-07 after UU-06 closure.

This execution is bounded to UU-07 only. It is implementing:

- PAIA owner-input as an authenticated adapter into the existing shared Semantic Intake kernel;
- the current-chat MCP Semantic Intake contract without a parallel business mutation path;
- platform-neutral ReminderIntent / reminder outbox contracts;
- truthful ChatGPT Tasks / Google Calendar capability probing;
- one delivery owner per ScheduleNode version/purpose reminder;
- source/version replay and cross-source semantic fact dedupe.

PAIA source type authorization does not silently grant any concrete PAIA client.
A delegated PAIA client must still authenticate and hold an explicit source-scoped
`semantic_intake` grant. External task/calendar adapters default to unsupported until
the PJSDAS runtime can actually call an authorized provider.

No UU-08 work is authorized by this checkpoint.
